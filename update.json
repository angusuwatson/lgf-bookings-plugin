{
    "name": "LGF Bookings",
    "slug": "simple-hotel-crm",
    "version": "1.9.4.34",
    "homepage": "https://github.com/angusuwatson/lgf-bookings-plugin",
    "download_url": "https://github.com/angusuwatson/lgf-bookings-plugin/archive/refs/heads/master.zip",
    "requires": "6.0",
    "tested": "6.8",
    "requires_php": "7.4",
    "last_updated": "2026-08-15",
    "upgrade_notice": "Fix: ICS import ignores cancelled and trashed bookings entirely, so a new booking imports even when an old cancelled/trashed one covers the same room and dates — as a fully fresh booking with its own guest.",
    "sections": {
        "description": "WordPress-native hotel CRM with calendar, bookings, guests, Invoice Ninja integration, and GitHub-based updating.",
        "changelog": "<ul><li>1.9.4.34: PWA — calendar Quick Booking modal now always shows editable guest detail fields (first name, last name, phone, email, city, country) with the guest search at the top, so a brand-new guest can be typed in directly from the calendar. Picking an existing guest from search still autofills the fields and links the booking.</li><li>1.9.4.33: PWA — fixed Room Day Notes on the calendar Edit Booking modal: closing/reopening no longer shows 'No room nights'. The modal now loads the booking's room nights even when opened from the calendar page, so existing saved notes are always displayed and can be edited. Also fixed the main plugin's calendar popup — a Returning guest search field was added for add-booking and the popup, and guest details fields are preserved.</li><li>1.9.4.32: Fix — ICS import treats cancelled and trashed bookings as if they don't exist. A new Booking.com booking now imports normally even when an old cancelled/trashed booking covers the same room and dates: the guest lookup ignores bookings that are deleted or cancelled (a fresh guest is created instead of reusing the old one), the primary booking lookup ignores cancelled bookings, the room insert frees the legacy_reserved_room_id still held by cancelled/trashed bookings' room rows so the UNIQUE constraint no longer silently drops the new room and nights, and the overlap sweep only counts active bookings so the freshly created closure is never swept as a duplicate.</li><li>1.9.4.31: Fix — new Booking.com bookings no longer inherit the guest of the old cancelled booking. The ICS import reuses a guest only when it belongs to an active booking with the same source ID; cancelled bookings are ignored, so a fresh closure gets its own guest exactly like any new import.</li><li>1.9.4.30: Fix — a cancelled booking can no longer block a new Booking.com booking from importing. The ICS import previously matched feed events against cancelled bookings, so when an old reservation was cancelled and a new one arrived for the same room and dates, the new closure was treated as already-imported and never appeared. The import lookup (and the room-less skeleton adoption fallback) now only matches active bookings, and the overlap cleanup no longer deletes a confirmed closure that is merely covered by a cancelled booking. New bookings are created even when a cancelled booking exists for the same room and dates.</li><li>1.9.4.29: Fix — two bugs behind “some rooms not importing” from Booking.com. First, when one room’s ICS feed fails to fetch (timeout/HTTP error), its events are absent from the seen-UID list, so the sweep cancelled every confirmed skeleton in that room. Now the sweep is skipped entirely whenever any feed fails; closures are only cancelled when every feed fetched OK, so a genuinely-removed closure still disappears on the next healthy run. Second, room-less skeleton bookings (no booking_rooms row) were invisible to the import lookup, so a fresh room-linked copy was created on every run while the ghost lingered. The lookup now adopts a room-less skeleton with the same source ID (linking its room), and the orphan sweep deletes any room-less skeleton whose source ID duplicates a room-linked one.</li><li>1.9.4.28: Fix — Square payment-complete handler now accepts $skip_invoice. Receipt reprint webhook passes it, so reprinting a receipt no longer triggers a second Invoice Ninja invoice.</li><li>1.9.4.27: UI — calendar Quick Edit Booking popup: taller and now scrolls when content is too long, fields arranged in 3 columns on desktop (1 column on mobile), and the Internal notes box is 3 lines tall instead of 5.</li><li>1.9.4.26: Fix — MotoPress status now syncs two ways. Previously only cancellations propagated into the CRM: a booking cancelled in MotoPress was cancelled in LGF but, if re-confirmed in MotoPress, stayed cancelled forever. Now re-confirming a booking in MotoPress restores it to confirmed in the CRM and it reappears on the calendar. The calendar cache is cleared whenever a status flips.</li><li>1.9.4.25: Fix — orphaned ICS skeleton bookings whose room link is broken (room 0 in the bookings list) are now cleaned up during each sync. The sweep only removes [ICS_SKELETON] bookings whose room_id no longer resolves; valid-room skeletons (enriched or not) are untouched. Also fixed the debug-ics tool so it can actually display CRM bookings (was querying a nonexistent column, always returning an empty list).</li><li>1.9.4.24: Fix — ICS import no longer duplicates skeleton bookings. Booking lookup now prefers enriched bookings over skeletons (previously a gap skeleton could be matched and expanded to the full ICS block), the gap existence check is now overlap-based instead of exact-date, and a cleanup routine removes fully-covered duplicate skeletons.</li><li>1.9.4.23: Fix — PWA service worker cached same-origin API responses cache-first, so calendar/pricing/room-status views served stale data forever. SW now bypasses /wp-json entirely, cache bumped to v3 with skipWaiting + clients.claim for immediate takeover. apiGet now adds automatic _=Date.now() cache-buster to every GET.</li><li>1.9.4.22: Fix — Invoice Ninja duplicate invoices. Invoice creation now atomically claims the booking (concurrent webhooks/buttons can no longer both create), rejects cancelled/deleted bookings, handles the '' vs NULL invoice-ID mismatch in the bulk sync, Square payment completion is idempotent per checkout, and the temporary Clear Invoice ID button was removed.</li><li>1.9.4.20: Fix — SIMPLE_HOTEL_CRM_VERSION now reads from plugin header via get_file_data(), so calendar and settings always show the current version. Merge candidates grouped by guest name (CLOSED grouped by dates only).</li><li>1.9.4.19: Fix — ICS skeleton existence check now joins booking_rooms table to filter by room_id, preventing skeletons from being skipped due to bookings in other rooms with the same dates.</li><li>1.9.4.18: Feature — Country field from guests table shown in calendar popup; Copy All format includes country code, channel+date, and blanks for zero values.</li><li>1.9.4.17: Fix — Today button scroll race condition fixed. Copy All button added to calendar popup.</li><li>1.9.4.16: UI — Today button now scrolls to today column. Restored version label in year nav.</li><li>1.9.4.15: UI — Calendar: added Today button, stronger today highlight, removed fullscreen toggle, centered year nav.</li><li>1.9.4.14: Fix — ICS skeleton creation now checks for any existing booking (not just skeletons) before creating new ones, preventing duplicates when enriched bookings overlap uncovered date ranges.</li><li>1.9.4.13: Fix — ICS sync overlap fallback now re-links enriched bookings when Booking.com changes the event UID, preventing duplicate bookings after manual edits.</li><li>1.9.4.12: Fix — ICS primary match now skips deleted bookings (prevents merge creating stale matches). Calendar shows version label, today column highlighted, removed autoscroll memory.</li><li>1.9.4.11: Fix — ICS skeleton ranges narrowed to avoid overlapping existing bookings in the same room.</li><li>1.9.4.10: Fix — ICS skeleton bookings now create a dedicated guest. Popup save strips [ICS_SKELETON] marker so editing guest name/phone/occupancy marks the booking as human-managed.</li><li>1.9.4.9: Fix — ICS sync: when Booking.com merges consecutive reservations into a single block, skeleton bookings are now created for date ranges before and after the enriched booking that fall within the block.</li><li>1.9.4.8: Fix — ICS sync: scoped legacy_reserved_room_id lookup to current booking so consecutive same-UID reservations for the same room each get their own room rows.</li><li>1.9.4.7: Fix — ICS sync: external_booking_id now includes dates to separate consecutive bookings with same UID. Primary match also requires date overlap to prevent UID collisions.</li><li>1.9.4.6: Fix — ICS sync: external_booking_id now includes dates to separate consecutive bookings with same UID. Primary match also requires date overlap to prevent UID collisions.</li><li>1.9.4.5: Fix — calendar popup Transfer booking button now wired up with guest search + confirm + REST call. Also fixed missing roomDayExtrasUrl in localized script data.</li><li>1.9.4.4: Fix — check-out auto-date now uses UTC arithmetic (fixes TZ bug where +1 day gave same day in UTC+2). Also fixed same bug in PHP server-side.</li><li>1.9.4.3: Fix — added console.error logging to shcFetchAvailableRooms to diagnose empty room dropdown.</li><li>1.9.4.2: Fix — 'room not found' error now shows the room_id and room_code from the CSV row.</li><li>1.9.4.1: Fix — CSV import booking-rooms error message split: now shows 'booking not found' vs 'room not found' separately.</li><li>1.9.4.0: Feat — Add Booking page fully AJAX: date change fetches rooms without reload, add/remove rooms client-side, guest search improved. New REST endpoint GET /available-rooms.</li><li>1.9.3.18: Fix — bookings list column widths tightened; headers shortened (Comm., Extras). Invoice Ninja Clear ID button in booking detail.</li><li>1.9.3.17: Feat — bookings list: invoice actions use AJAX (no page reload). Removed unused reserved_room_id from REST API.</li><li>1.9.3.16: Feat — Invoice Ninja: itemised extras with product lookup, invoice status sync + column, refresh button. Catalog CRUD UI in Settings. Renamed Dinner to Diner (17.50€). Fix — catalog code moved from booking detail page to correct Settings tab.</li><li>1.9.3.15: Fix — calendar: renamed Rooms row to Room rate (monetary total), added rate_day field. Discount value input starts empty.</li><li>1.9.3.14: Feat — calendar: added Extras, Rooms, Commission footer rows below Income Day. Booking detail: removed ID and Room note columns from rooms table; discount value input starts empty.</li><li>1.9.3.13: Fix — bookings list: replaced Source ID column with Room Day Extras (sum of extras_amount from room nights) to identify bookings with nightly extras for phase-out.</li><li>1.9.3.12: Fix — calendar numeric cells right-aligned; income day includes room rates + extras; tarif row fully bold.</li><li>1.9.3.11: Feature — calendar totals column on the far right shows monthly sums for extras, room rate, commission, income, adults, and children. Income Day now includes room rates + extras. All summary rows right-aligned. Room rate row fully bold including totals cell.</li><li>1.9.3.10: Fix — booking_items.booking_room_id no longer orphaned after room re-insertion; existing extras items now editable (room/date) in the booking detail page.</li><li>1.9.3.9: Fix — overlap fallback merges only into skeleton bookings (not enriched), preventing accidental merging of distinct reservations.</li><li>1.9.3.8: Fix — added overlap-by-room+date fallback lookup when ICS UID fails to match; handles Booking.com hash-based UIDs that change on every event modification.</li><li>1.9.3.7: Fix — ICS sync now respects STATUS:CANCELLED from ICS feed; cancelled events are no longer imported as confirmed. source_booking_id column added to booking list admin page for debugging.</li><li>1.9.3.6: Fix — quick-booking GET room lookup now matches POST pattern (legacy_reserved_room_id OR id, by booking_id). Cache-busting timestamp added to all REST GET calls to prevent stale server-cached responses. Modal close ordered before calendar refresh to avoid UI flicker.</li><li>1.9.3.5: Fix — changing occupancy on the calendar popup now recalculates room pricing. The quick-booking POST handler calls simple_hotel_crm_calculate_room_pricing() after writing occupancy, updating all price columns in booking_rooms, booking_room_nights, and bookings.</li><li>1.9.3.4: Fix — calendar extras total now includes Extras & Charges items from booking_items table (room-line extras + line-item extras). Items are matched by booking_id, booking_room_id, and stay_date for per-cell accuracy.</li><li>1.9.3.3: Fix — saving a booking from the booking detail page no longer orphans room day notes (night-level). Day notes are now migrated from old to new booking_room_id after room re-insertion.</li><li>1.9.3.2: Fix — calendar transient cache key now includes plugin version. Room day notes and extras amount display fresh data instead of stale 30-minute cache after every update.</li><li>1.9.3.1: Fix — quick-booking GET occupancy now reads from booking_room_nights (night-level) instead of booking_rooms.adults which may be 0. Uses LEFT JOIN + GROUP BY + MAX to match calendar display.</li><li>1.9.3.0: Fix — quick-booking GET now always fetches booking_rooms by booking_id (no reserved_room_id filter that missed rooms). POST lookup matches on both legacy_reserved_room_id and booking_rooms.id.</li><li>1.9.2.99: Calendar popup occupancy now only reads/writes booking_rooms table (not booking-level fields). Saves to specific room via reserved_room_id.</li><li>1.9.2.98: Fix — occupancy populates from booking_rooms table (per-room data) instead of booking-level fields which are often 0.</li><li>1.9.2.97: Occupancy editing (adults/children/babies) moved to the calendar's Quick Edit Booking modal — click a guest name on the calendar, set room occupancy, save. Quick-booking GET endpoint returns adults/children/babies. POST endpoint saves to bookings, booking_rooms, and booking_room_nights tables.</li><li>1.9.2.96: Calendar booking popup now shows editable room occupancy fields (adults, children, babies) per room.</li><li>1.9.2.95: Rework MotoPress sync room handling — orphan bookings are no longer deleted. They are kept with existing data (guest, dates) and room import is retried non-destructively on every sync. Room mapping errors are clearly logged to Sync Log per booking ID. Original booking headers preserved, never rolled back or deleted.</li><li>1.9.2.94: Fix MotoPress sync not creating occupancy — better error diagnostics logged per booking ID in sync log. Added room retry for orphan bookings.</li><li>1.9.2.93: Consolidated Settings to 4 tabs (General, Channels, Payments, Data). Room Closures + Tickets are now tabs on Bookings page. Hidden submenu items removed. MotoPress, Booking.com, Invoice Ninja, ICS Export, Sync Log merged under Channels tab. Import + Export merged under Data tab. POST handlers use fallback-to-existing-value to prevent zeroing settings.</li><li>1.9.2.92: Fix cancelled bookings still blocking rooms in ICS export — ICS feeds now regenerate when booking status changes to cancelled, removing stale blocked events from the .ics file.</li><li>1.9.2.91: Fix Square webhook signature verification — include notification URL in HMAC per Square spec.</li><li>1.9.2.90: New Stay Purpose field — track booking categories (pilgrim/hiker, cyclist, work, etc.). Editable list under Stay Purposes submenu. Stay Purpose dropdown on add-booking and booking detail forms. DB bumped to 25.</li><li>1.9.2.89: Calendar now shows checked_in and checked_out bookings alongside confirmed.</li><li>1.9.2.88: Booking detail form now editable — booking-wide note textarea, room-note input per room. Add-booking: room-note field added. Day-specific notes no longer wiped on booking save. Calendar only shows day-specific notes per cell (no fallback).</li><li>1.9.2.87: ICS sync no longer reuses guests by phone+name match — each unique booking gets its own guest.</li><li>1.9.2.86: Calendar year navigation — prev/next year arrows keep same month, switch year. AJAX loading supported.</li><li>1.9.2.85: Room closures now editable — Edit link in Actions column, form pre-fills dates/reason/room, Cancel returns to blank form.</li><li>1.9.2.84: New guest_preferences table + admin UI. Guest lookup (name/email/phone search) on add-booking page auto-fills form. Preferences shown on booking detail page. ICS import no longer pollutes guests.notes. create_wp_crm_booking() accepts optional guest_id to update existing guests.</li><li>1.9.2.83: Fix nonce failure causing blank page — replaced check_admin_referer with manual wp_verify_nonce. Date auto-advance wrapped in DOMContentLoaded for reliability.</li><li>1.9.2.82: Auto-advance date_to picker after choosing date_from. Darker closed cell bg (#999).</li><li>1.9.2.81: Calendar closed cells — room-name row not greyed out, darker grey bg, closure reason shown in guest field.</li><li>1.9.2.80: Room Closures page — room selector dropdown for single-room close, bulk delete with checkboxes.</li><li>1.9.2.79: Fix room closures table not being created — bumped DB version to 23, added runtime table check in admin page, added database error reporting, closures now included in ICS exports.</li></ul>"
    }
}
